'use client';import { useActionState } from 'react';import { adminLogin } from '@/actions/auth';import { Button,Input } from '@/components/ui';export default function Page(){const[s,a,p]=useActionState(adminLogin,{} as any);return <main className="grid min-h-screen place-items-center paper-grain"><form action={a} className="w-full max-w-sm rounded-3xl bg-white p-8 shadow-premium"><h1 className="text-3xl font-black">Admin</h1><Input name="password" type="password" placeholder="Password" className="mt-5"/><Button disabled={p} className="mt-4 w-full">Login</Button>{s?.error&&<p className="mt-3 text-red-600">{s.error}</p>}</form></main>}
