import { requireLeader } from '@/lib/auth';import { prisma } from '@/lib/prisma';import { logout } from '@/actions/auth';import LeaderClient from './ui';
export default async function Page(){await requireLeader();const cards=await prisma.card.findMany({include:{owner:true,logs:{take:5,orderBy:{timestamp:'desc'}}},orderBy:{createdAt:'desc'},take:100});return <main className="min-h-screen p-4 paper-grain"><form action={logout} className="mb-4 text-right"><button className="underline">Logout</button></form><LeaderClient cards={cards as any}/></main>}
