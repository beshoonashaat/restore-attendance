'use client';import { useActionState } from 'react';import { claimCard } from '@/actions/cards';import { Button,Input } from '@/components/ui';
export default function ClaimForm({uuid}:{uuid:string}){const [state,action,pending]=useActionState(claimCard,{} as any);return <main className="min-h-screen paper-grain p-6"><form action={action} className="mx-auto mt-24 max-w-md rounded-3xl bg-white p-8 shadow-premium"><h1 className="text-3xl font-black">Claim Your Card</h1><input type="hidden" name="uuid" value={uuid}/><label className="mt-6 block text-sm font-semibold">Full Name</label><Input name="fullName" required autoComplete="name"/><Button className="mt-5 w-full" disabled={pending}>Claim Card</Button>{state?.error&&<p className="mt-3 text-red-600">{state.error}</p>}{state?.ok&&<p className="mt-3 text-green-700">Claimed. Refreshing...</p>}</form></main>}
